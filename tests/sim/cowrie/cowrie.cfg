# Cowrie honeypot config for the notnet sim.
# Listens on the REAL service ports (22 ssh, 23 telnet) so the bot's
# scanner/spreader hits it directly. Logs go to /cowrie/log (JSON + text).
[ssh]
listen_endpoints = tcp:22:interface=0.0.0.0

[telnet]
listen_endpoints = tcp:23:interface=0.0.0.0

[shell]
filesystem = /cowrie/fs.pickle

[database_jsonlog]
logfile = /cowrie/log/cowrie.json

[log]
logfile = /cowrie/log/cowrie.log

[honeypot]
contents_path = /cowrie/honeyfs
download_path = /cowrie/dl
